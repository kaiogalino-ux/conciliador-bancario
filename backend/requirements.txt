# Dependências de execução do backend (é isto que entra na imagem Docker).
# Streamlit e pytest ficam em requirements-dev.txt para não inchar o servidor.

# Núcleo da conciliação (mesmas regras de sempre — ver CLAUDE.md).
pandas==3.0.3
numpy==2.5.1
openpyxl==3.1.5
xlrd==2.0.1
ofxparse==0.21
python-dateutil==2.9.0.post0
python-dotenv==1.2.2

# Camada de IA (importada de forma preguiçosa em src/ia_cliente_groq.py).
groq==1.5.0

# Servidor HTTP (api/).
fastapi==0.122.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
