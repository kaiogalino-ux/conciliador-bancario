# Ambiente de desenvolvimento local: tudo do servidor + testes + a interface
# Streamlit, que continua funcionando em paralelo à interface web.
#
#     pip install -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
httpx==0.28.1
streamlit==1.60.0
